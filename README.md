# DuckAI from OpenAI

**Converts OpenAI-compatible requests (only /chat/completions) to Duck.AI requests**

### DISCLAIMER: Since [Duck.AI](https://duck.ai) only supports streaming, `streaming: false` will be ignored

todo
